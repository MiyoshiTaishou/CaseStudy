実装内容

マウス入力でのホログラム作成
・壁
・床
・プレイヤー
生成したホログラムを時間経過で削除

マウスのクリックか範囲選択した場所にホログラムを作成するようにしました。
床と壁は範囲選択時の横の差分と縦の差分で大きい方を優先するようにしてます。
同じ場合は縦の壁が優先されます。
生成したホログラムは5秒で自動的に削除されます。時間指定はできます。
プロトに入れるのは競合こわいんでやってません。自分のフォルダに導入手順あるんで入れたかったらそれ見てやってください。