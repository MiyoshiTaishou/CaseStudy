using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class N_Test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //SEnemyMove.iTest = 0;

    }

    // Update is called once per frame
    void Update()
    {

        //Debug.Log(SEnemyMove.iTest);
        //SEnemyMove.iTest = 0;
    }
}
